Таким образом, конструкция $var =~ s/ ... / ... / изменяет значение пере
менной $var (впрочем, если совпадение не обнаружено, замена не произ
водится и переменная сохраняет прежнее значение). Например, если пе
ременная $var содержит строку Jeff • Friedl, то при выполнении команды
$var =~ s/Jeff/Jeffrey/;
